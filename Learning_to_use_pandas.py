import pandas as pd
d = {
     'Machine Learning Tasks': ['Clustering', 'Classification', 'Regression Analysis', 'Dimensionality Reduction'],
     'Example of algorithms': ['K-means', 'SVM', 'Linear Regression', 'PCA']}
df = pd.DataFrame(data=d)
df
